package valencia.java;

import java.awt.*;
/*
public class SquareSpiral {
    public static void main(String[] args) {
        Point lastPoint = new Point(0,0);
        for (int i = 0; i < 1000; i++) {
            Point point = new Point((int)(Math.random() * 400) - 200, (int)(Math.random() * 400) - 200);
            valencia.java.turtleInheritance.Turtle.Turtle bob = new Turtle.Turtle(point.x,point.y);



            bob.fillColor(buildColor());
            bob.face(lastPoint.x, lastPoint.y);
            bob.forward(getRandom());
            lastPoint = new Point((int) bob.getX(),(int) bob.getY());



        }

    }

    private static Color buildColor() {
        return (new Color(getRandom(), getRandom(), getRandom()));
    }

    private static int getRandom() {
        return (int) (Math.random() * 256);
    }
}*/
